# Secrets
.env

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# Ingested data (derived, regenerated by run_daily --to-file; not source)
data/

# Triage predictions written by the /digest session for `make eval` (derived, not source;
# the founder's ground-truth labels live in evalset/labels.csv, which IS tracked)
evalset/predictions.jsonl

# OS noise
.DS_Store
